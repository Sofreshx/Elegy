# Extraction Matrix — SAASTools formalization -> Elegy burden-of-proof reset

Scope: workflow formalization artifacts already changed or planned in SAASTools, including workflow models and sharing, WS3 governance scripts, and agent-orchestration-adjacent shapes.

Companion note: the phase-1 reusable memory/context split is tracked in [rust/features/elegy-memory/docs/migration/reusable-memory-boundary.md](../rust/features/elegy-memory/docs/migration/reusable-memory-boundary.md). Apply the same governed-artifacts-first rule there: portable envelopes, metadata, fixtures, manifests, and local non-authoritative import/list/show/export/supersede/tombstone semantics may graduate into `Elegy`, while currentness, approval, freshness policy, retrieval ranking, runtime validation, promotion, persistence, and product integration stay consumer-local in `SAASTools`.

Decision rule: do not move code into Elegy unless it clearly proves why shared ownership is needed at all. Every candidate should be classified into one of these lanes:

- `governed artifacts` — canonical schemas, fixtures, manifests, policies, or machine-readable exports that downstream consumers must consume consistently
- `Rust executable` — reusable executable or tooling behavior under `rust/`
- `consumer-local` — host-specific runtime behavior that should stay in SAASTools or another app repo
- `docs/workflows` — shared docs, scripts, or CI definitions that do not need a runtime library

Topology note: the current Elegy repo is centered on governed root artifacts plus the Rust workspace. Extraction targets in this matrix therefore map to `contracts/`, root scripts and workflows, or `rust/` crates. They do **not** map to a removed `src/` or `.NET` package-family tree. The `src/Elegy-*/install.ps1` wrapper installer lanes were retired; the canonical installer at `scripts/install-distribution.sh` is the install path. Plugin packages (`elegy-plugin-package/v1`) have been retired in favor of the minimal `elegy-plugin/v1` manifest format.

| Source file | Disposition | Shared-code proof | Execution lane | Target in Elegy (or reason kept) | Rationale | API or handoff mapping | Validation impact notes |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `docs/planning/index.md#memory-and-context-substrate-status` | re-evaluate | Only the governed reusable-memory artifacts and local non-authoritative artifact-management semantics clear the shared-boundary bar; runtime authority does not. | `governed artifacts` | Keep the boundary and release posture in `rust/features/elegy-memory/docs/migration/reusable-memory-boundary.md` and future `contracts/` assets when the family is actually published. | This preserves the authority split: `Elegy` may describe local import/list/show/export/supersede/tombstone semantics, while `SAASTools` keeps currentness, approval, freshness policy, retrieval ranking, runtime validation, and promotion. | `SAASTools` memory roadmap -> bounded governed memory-family metadata only | No runtime extraction in this slice; keep runtime/store/promotion validation in `SAASTools`. |
| `Libraries/Workflow/WorkflowModels.cs` | move | Canonical workflow semantics should be shared consistently, but the active shared form is artifact-first rather than a revived source package. | `governed artifacts` | Existing workflow contract assets under `contracts/schemas/canonical-workflow.schema.json`, `contracts/schemas/canonical-workflow-graph.schema.json`, and matching fixtures; Rust consumers read those through `rust/core/elegy-contracts` when executable use is needed. | This is real authority, but today that authority is carried by governed contracts and fixtures. | App-local workflow types -> governed workflow schemas and exported bundle artifacts | Keep contract-bundle and downstream-consumer validation aligned with the workflow schemas. |
| `Libraries/Workflow/WorkflowService.cs` | keep | Only the persistence-free normalization pieces may prove reusable; the current service is still app-shaped. | `consumer-local` | Keep in SAASTools for now. | This file mixes orchestration and app behavior; it does not clear the shared-boundary bar as a whole. | No immediate Elegy replacement; consume governed workflow artifacts directly where possible. | Keep orchestration and persistence-adjacent tests in SAASTools. |
| `Libraries/Workflow/Sharing/WorkflowShareExport.cs` | re-evaluate | A stable exchange contract could belong in Elegy, but only if multiple consumers truly need the same published artifact. | `governed artifacts` | If promoted, add a new schema-and-fixture pair under `contracts/`; until then keep the shape local. | There is no need to invent a new shared surface until the export contract is proven durable. | App-local export DTO -> governed schema only after cross-repo demand is confirmed | Keep endpoint and redaction tests local until a governed export contract exists. |
| `Libraries/Workflow/Sharing/WorkflowMermaidCodec.cs` | keep | The current proof is a projection helper, not a durable authority surface. | `consumer-local` | Keep in SAASTools unless a second strong consumer proves a reusable export or CLI need. | Useful output does not automatically justify long-term shared ownership. | Keep local codec or revisit later as a Rust tooling output if demand grows. | Keep deterministic output checks local for now. |
| `Api/Tools.Api/Features/Workflows/WorkflowValidationContract.cs` | keep | Query/body/header precedence is transport-facing caller logic, not canonical workflow authority. | `consumer-local` | Keep in SAASTools. | Endpoint-shaped validation is not the same thing as governed workflow semantics. | Local transport resolver -> governed contracts only where semantic validation is shared | Keep endpoint-level tests in SAASTools. |
| `Api/Tools.Api/Features/Workflows/WorkflowSharingEndpoints.cs` | adapter | Shared code is justified only for contracts or reusable tooling, not for HTTP endpoint ownership. | `consumer-local` | Keep endpoint local and consume governed artifacts where needed. | Auth, tenancy, and transport remain app concerns. | Local endpoint wrapper over exported contracts | Keep endpoint tests in SAASTools. |
| `Api/Tools.Api/Features/Workflows/WorkflowEndpoints.cs` | adapter | Shared code may exist for schemas or reusable tooling, but not for the endpoint wrapper itself. | `consumer-local` | Keep endpoint local and consume retained Elegy authority surfaces only. | Validation and pinning transport behavior stay with the app. | Local endpoint wrapper over governed workflow contracts | Keep auth and endpoint tests in SAASTools. |
| `Api/Tools.Api/Features/Workflows/WorkflowExecutionEndpoints.cs` | keep | No shared-code proof; this is explicit product runtime behavior. | `consumer-local` | Keep in SAASTools. | Execution and cancellation are host responsibilities. | No Elegy replacement planned | Retain execution-path tests in SAASTools. |
| `Api/Tools.Api/Features/Workflows/WorkflowInputOverrideEndpoints.cs` | keep | No shared-code proof; depends on app stores and trigger lookup. | `consumer-local` | Keep in SAASTools. | Endpoint depends on product execution context. | No Elegy replacement planned | Retain local override tests. |
| `.github/policies/visual-llm-formalization-enforcement-policy.json` | done | Single governed operational policy artifact, not duplicated repo-local state. | `docs/workflows` | `docs/governance/visual-llm-enforcement-policy.json` | Centralized policy source-of-truth is justified even without a runtime library. | Policy file path changed to `docs/governance/` | Validate policy shape and usage in Elegy CI. |
| `scripts/ws3/resolve-enforcement-mode.ps1` | move | Reusable governance tooling is justified as shared repo tooling rather than app-local duplication. | `docs/workflows` | `scripts/ws3/resolve-enforcement-mode.ps1` when that script family is imported | Deterministic policy resolution belongs with the governed policy source. | App-local governance script -> central Elegy script | Add focused script coverage when imported. |
| `scripts/ws3/evaluate-formalization-gates.ps1` | move | Same proof as policy resolution: shared repo tooling, not app-local duplication. | `docs/workflows` | `scripts/ws3/evaluate-formalization-gates.ps1` when imported | Gate evaluation should stay coupled to the central policy implementation. | App-local governance script -> central Elegy script | Add gate-behavior checks when imported. |
| `scripts/ws3/write-mode-audit-artifact.ps1` | move | Shared audit artifact composition is justified as governance tooling. | `docs/workflows` | `scripts/ws3/write-mode-audit-artifact.ps1` when imported | Keeps audit schema and generation logic near the policy source. | App-local governance script -> central Elegy script | Add audit-output validation when imported. |
| `scripts/ws3/README.md` | move | Shared operational guidance belongs with the shared governance tooling. | `docs/workflows` | `scripts/ws3/README.md` or a governance doc under `docs/` when imported | Prevents drift between scripts and docs. | Docs path change only | Keep link and usage guidance aligned with imported scripts. |
| `.github/workflows/ws3-formalization-governance.yml` | adapter | Shared workflow definition is justified; repo-specific invocation stays local. | `docs/workflows` | Existing `Elegy/.github/workflows/ws3-formalization-governance.yml` as canonical reusable workflow | CI orchestration is repo-specific, but the governance job definition should be centralized. | App-local inline job -> reusable workflow invocation | Validate reusable-workflow invocation in consuming repos. |
| `Libraries/SAASTools.Abstractions/Agents/AgentRequestAnalysis.cs` | re-evaluate | If retained, it should become a governed exchange contract, not proof of a large standalone agent library. | `governed artifacts` | Prefer existing agent envelopes under `contracts/` where they fit; otherwise add a new governed schema only after the boundary is proven. | The shape is a boundary concern first, not an automatic library candidate. | App-local DTO -> governed schema or envelope artifact | Move only contract-focused validation if the shape graduates into `contracts/`. |
| `Libraries/SAASTools.DesktopHost/Agent/Orchestration/HeuristicAgentRequestAnalyzer.cs` | keep | No shared-code proof yet; this is host-specific classification behavior. | `consumer-local` | Keep in SAASTools until multiple consumers prove a shared orchestration boundary. | Deterministic does not automatically mean shared. | No immediate Elegy target | Keep orchestration tests in SAASTools. |
| `Libraries/SAASTools.DesktopHost/Agent/Orchestration/DesktopOrchestratorRouter.cs` | keep | No shared-code proof yet; prompt assembly is host-local. | `consumer-local` | Keep in SAASTools. | Prompt policy composition is specific to the current host and product shell. | No immediate Elegy target | Keep router tests in SAASTools. |
| `Libraries/SAASTools.DesktopHost/Agent/Orchestration/BuiltInOrchestratorRegistrationService.cs` | keep | Catalog and build logic have not yet proved a host-agnostic shared boundary. | `consumer-local` | Keep in SAASTools until a real shared construction boundary is proven. | Avoid preserving a weak shared factory abstraction prematurely. | No immediate Elegy target | Keep registration tests in SAASTools. |
| `Libraries/SAASTools.DesktopHost/Agent/DesktopAgentDispatcher.cs` | keep | No shared-code proof; dispatcher ownership is inherently host-local. | `consumer-local` | Keep in SAASTools. | Dispatcher should orchestrate runtime locally and depend only on proven shared primitives. | No immediate Elegy target | Keep integration tests in SAASTools. |
| `Libraries/SAASTools.DesktopHost/Program.cs` | keep | No shared-code proof; composition root is always app-local. | `consumer-local` | Keep in SAASTools. | DI composition belongs to the product host. | No Elegy replacement planned | Add local startup validation as needed. |

## Execution ordering (recommended)

1. Move real authority artifacts first: schemas, fixtures, manifests, and policy files that clearly need a single source of truth.
2. Move reusable executable behavior into Rust crates only when the behavior is genuinely shared and validated.
3. Keep host-specific orchestration, dispatch, endpoints, and DI local unless at least two real consumers prove a stronger shared boundary.
4. Point contributors at the current Elegy validation path: root export and canonical-output scripts plus Rust workspace checks, not removed solution-era flows.
