# Extraction Matrix — SAASTools formalization -> Elegy

Scope: workflow formalization artifacts already changed/planned in SAASTools (workflow models/sharing/validation contract, WS3 governance scripts, agent-orchestration primitives).

| Source file | Disposition (move\|keep\|adapter) | Target in Elegy (or reason kept) | Rationale | API replacement mapping (old surface -> new Elegy surface) | Test impact notes |
|---|---|---|---|---|---|
| `Libraries/Workflow/WorkflowModels.cs` | move | `src/Elegy.Formalization/Workflow/Models/WorkflowDefinition.cs` | Contains canonical workflow schema and blueprint governance metadata (`SpecVersion`, `CanonicalAuthority`, `ConflictPolicy`, pin metadata) that should live in formalization core. | `SAASTools.Workflow.WorkflowDefinition` -> `Elegy.Formalization.Workflow.Models.WorkflowDefinition`; `BlueprintMetadata` -> `Elegy.Formalization.Workflow.Models.BlueprintMetadata` | Port governance-default assertions from `Api/Tools.Api.Tests/Features/Workflows/WorkflowGovernanceMetadataDefaultingTests.cs`; add model round-trip tests in Elegy. |
| `Libraries/Workflow/WorkflowService.cs` | adapter | `src/Elegy.Formalization/Workflow/Services/WorkflowGovernanceService.cs` (pure normalization/clone logic); keep Marten persistence in SAASTools | File mixes persistence and formalization rules; extract only pure rule logic and keep data-access orchestration local. | `WorkflowService.NormalizeWorkflowDefinition/CloneBlueprintMetadata` -> `IWorkflowGovernanceService.Normalize(...)`; `CloneStepsWithIdMap/CloneConnections/CloneLayoutData` -> `IWorkflowGovernanceService.CloneForTenant(...)` | Split current tests: move pure logic checks from `WorkflowGovernanceMetadataDefaultingTests` to Elegy; keep SAASTools integration tests for Marten CRUD behavior. |
| `Libraries/Workflow/Sharing/WorkflowShareExport.cs` | move | `src/Elegy.Formalization/Workflow/Sharing/WorkflowShareExport.cs` | Defines sanitized formalization exchange payload and projection metadata (`workflow-share-export`, projection formats). | `WorkflowShareExport.FromDefinition/ToDefinition` -> `Elegy.Formalization.Workflow.Sharing.WorkflowShareExport.FromDefinition/ToDefinition` | Add Elegy tests for secret redaction and representation metadata; SAASTools endpoint tests continue through adapter. |
| `Libraries/Workflow/Sharing/WorkflowMermaidCodec.cs` | move | `src/Elegy.Formalization/Workflow/Sharing/WorkflowMermaidCodec.cs` | Deterministic mermaid projection is reusable formalization output, not product-specific API behavior. | `WorkflowMermaidCodec.Serialize(...)` -> `Elegy.Formalization.Workflow.Sharing.WorkflowMermaidCodec.Serialize(...)` | Move deterministic serialization checks from `Api/Tools.Api.Tests/WorkflowMermaidCodecTests.cs` into Elegy; keep one SAASTools smoke test for endpoint wiring. |
| `Api/Tools.Api/Features/Workflows/WorkflowValidationContract.cs` | move | `src/Elegy.Formalization/Validation/WorkflowValidationModeResolver.cs` | Encapsulates cross-surface validation precedence contract (query > body > header > default) and deterministic error shape. | `WorkflowValidationContract.TryResolve(...)` -> `WorkflowValidationModeResolver.Resolve(...)` | Move resolver unit coverage from `WorkflowValidationContractTests` to Elegy; keep endpoint-level status code contract tests in SAASTools. |
| `Api/Tools.Api/Features/Workflows/WorkflowSharingEndpoints.cs` | adapter | `src/Elegy.Formalization/Application/IWorkflowFormalizationFacade.cs` | Endpoint should remain auth/tenant HTTP boundary; formalization conversion/validation/export logic should be delegated. | `/workflows/{id}/export*`, `/workflows/validate-text` handlers -> `IWorkflowFormalizationFacade.ExportShare/ExportText/ExportMermaid/ValidateText` | Existing endpoint tests remain in SAASTools but mock facade; core behavior tests move to Elegy facade tests. |
| `Api/Tools.Api/Features/Workflows/WorkflowEndpoints.cs` | adapter | `src/Elegy.Formalization/Application/IWorkflowFormalizationFacade.cs` + `src/Elegy.Formalization/Workflow/Governance/PinningService.cs` | `validate`, `pin`, and `unpin` behavior belongs to formalization governance; endpoint remains transport/auth wrapper. | `/workflows/{id}/validate` -> `ValidateDefinition(...)`; `/workflows/{id}/pin|unpin` -> `PinningService.Pin/Unpin` | Keep endpoint auth/HTTP tests in SAASTools; move pin/unpin rule tests into Elegy governance suite. |
| `Api/Tools.Api/Features/Workflows/WorkflowExecutionEndpoints.cs` | keep | Keep in SAASTools (`Tools.Api` execution runtime + tenant/role checks) | Runtime execution, cancellation, recovery, and override authorization are API/runtime concerns, not reusable formalization core. | Optional dedupe only: local `CanOverrideStep/HasCustomBoolClaim` can call future `Elegy.Orchestration.Authorization.OverridePolicy` later; no immediate extraction. | Retain `Api/Tools.Api.Tests/Features/Workflows/WorkflowOverrideAuthorizationTests.cs` in SAASTools. |
| `Api/Tools.Api/Features/Workflows/WorkflowInputOverrideEndpoints.cs` | keep | Keep in SAASTools (`Tools.Api` + `IWorkflowInputOverrideStore`) | Endpoint depends on SAASTools tenant stores and workflow trigger schema retrieval. | Keep current endpoint surface; if shared policy emerges, map claim parsing helper to shared package only. | Retain and expand `WorkflowOverrideAuthorizationTests` for input key resolution and RBAC edge cases. |
| `.github/policies/visual-llm-formalization-enforcement-policy.json` | move | `policies/formalization/visual-llm-enforcement-policy.json` | Policy document is governance source-of-truth and should be owned with formalization governance runtime. | `./.github/policies/visual-llm-formalization-enforcement-policy.json` -> `Elegy/policies/formalization/visual-llm-enforcement-policy.json` | Add policy schema validation test in Elegy CI; SAASTools consumes published/copied policy artifact. |
| `scripts/ws3/resolve-enforcement-mode.ps1` | move | `scripts/ws3/resolve-enforcement-mode.ps1` | Deterministic policy resolution logic is governance core and should be centralized with policy source. | `pwsh ./scripts/ws3/resolve-enforcement-mode.ps1` (SAASTools) -> `pwsh <Elegy>/scripts/ws3/resolve-enforcement-mode.ps1` | Add Pester tests in Elegy for precedence and match-type behavior; remove duplicated script logic from SAASTools over time. |
| `scripts/ws3/evaluate-formalization-gates.ps1` | move | `scripts/ws3/evaluate-formalization-gates.ps1` | Gate decision logic (warn/strict/block) should remain coupled to formalization policy implementation. | `pwsh ./scripts/ws3/evaluate-formalization-gates.ps1` -> `pwsh <Elegy>/scripts/ws3/evaluate-formalization-gates.ps1` | Add Elegy tests for strict-mode nonzero exit and severity aggregation. |
| `scripts/ws3/write-mode-audit-artifact.ps1` | move | `scripts/ws3/write-mode-audit-artifact.ps1` | Audit artifact composition is governance reporting logic and should be emitted from same governance package/repo. | `pwsh ./scripts/ws3/write-mode-audit-artifact.ps1` -> `pwsh <Elegy>/scripts/ws3/write-mode-audit-artifact.ps1` | Add Elegy test validating merged audit schema (`policy`, `modeSelection`, `gateDecision`). |
| `scripts/ws3/README.md` | move | `docs/governance/ws3-scripts.md` (or `scripts/ws3/README.md`) | Operational docs should live with extracted governance scripts to avoid drift. | `SAASTools/scripts/ws3/README.md` -> `Elegy/docs/governance/ws3-scripts.md` | Update doc-link checks in both repos once paths switch. |
| `.github/workflows/ws3-formalization-governance.yml` | adapter | `Elegy/.github/workflows/ws3-formalization-governance.yml` as canonical; SAASTools keeps thin caller workflow | CI orchestration is repo-specific, but governance job definition should be centralized and reusable from Elegy. | SAASTools inline `ws3-governance` job -> `uses: <org>/Elegy/.github/workflows/ws3-formalization-governance.yml@<ref>` | Validate reusable-workflow invocation in SAASTools CI; full governance matrix tests run in Elegy CI. |
| `Libraries/SAASTools.Abstractions/Agents/AgentRequestAnalysis.cs` | move | `src/Elegy.Orchestration/Contracts/AgentRequestAnalysis.cs` | Request-kind/profile contract is a reusable orchestration primitive, not desktop-host-specific. | `SAASTools.Abstractions.Agents.AgentRequestAnalysis` + `IAgentRequestAnalyzer` -> `Elegy.Orchestration.Contracts.AgentRequestAnalysis` + `IRequestAnalyzer` | Move contract-focused tests from `Libraries/SAASTools.DesktopHost.Tests/AgentRequestOrchestrationTests.cs` into Elegy. |
| `Libraries/SAASTools.DesktopHost/Agent/Orchestration/HeuristicAgentRequestAnalyzer.cs` | move | `src/Elegy.Orchestration/Analysis/HeuristicAgentRequestAnalyzer.cs` | Deterministic classification heuristics are orchestration policy logic reusable across hosts. | `HeuristicAgentRequestAnalyzer.Analyze(string)` -> `Elegy.Orchestration.Analysis.HeuristicAgentRequestAnalyzer.Analyze(string)` | Move `HeuristicAgentRequestAnalyzerTests` to Elegy; keep one host integration test in SAASTools for DI wiring. |
| `Libraries/SAASTools.DesktopHost/Agent/Orchestration/DesktopOrchestratorRouter.cs` | move | `src/Elegy.Orchestration/Routing/OrchestratorPromptComposer.cs` | Prompt policy generation from request analysis + matched skills is core orchestration behavior. | `DesktopOrchestratorRouter.BuildSystemPromptAddendum(...)` -> `OrchestratorPromptComposer.Compose(...)` | Move `DesktopOrchestratorRouterTests` to Elegy; keep SAASTools smoke test asserting composed prompt is appended. |
| `Libraries/SAASTools.DesktopHost/Agent/Orchestration/BuiltInOrchestratorRegistrationService.cs` | adapter | `src/Elegy.Orchestration/Catalog/BuiltInOrchestratorCatalog.cs` | Contract list content should move to Elegy catalog; host service remains startup adapter for `IAgentRegistry`. | Inline hardcoded `IAgentContract[]` -> `BuiltInOrchestratorCatalog.GetContracts()`; service only registers returned contracts. | Keep `BuiltInOrchestratorRegistrationServiceTests` but switch assertions to catalog-fed registration; add Elegy tests for catalog IDs/capabilities. |
| `Libraries/SAASTools.DesktopHost/Agent/DesktopAgentDispatcher.cs` | adapter | `src/Elegy.Orchestration/Runtime/IOrchestrationPolicyEngine.cs` | Dispatcher should orchestrate runtime only; analysis/routing/prompt policy should be delegated to extracted engine. | `_requestAnalyzer.Analyze + _orchestratorRouter.BuildSystemPromptAddendum` -> `_policyEngine.BuildGuidance(userMessage, matchedSkills)` | Add SAASTools integration test for dispatcher-policy-engine wiring; move policy behavior tests into Elegy runtime tests. |
| `Libraries/SAASTools.DesktopHost/Program.cs` | keep | Keep in SAASTools (DesktopHost composition root) | DI composition is host-specific; only binding targets should change to Elegy implementations. | `AddSingleton<IAgentRequestAnalyzer, HeuristicAgentRequestAnalyzer>()` -> `AddSingleton<IRequestAnalyzer, ElegyHeuristicRequestAnalyzer>()`; `AddSingleton<DesktopOrchestratorRouter>()` -> `AddSingleton<IOrchestratorPromptComposer, ElegyOrchestratorPromptComposer>()` | Add startup DI validation test to ensure Elegy services resolve; no migration of host bootstrap tests needed. |

## Execution ordering (recommended)
1. Move pure formalization contracts/codecs (`WorkflowModels`, share export, mermaid codec, validation resolver) into Elegy.
2. Move WS3 policy + scripts into Elegy and switch SAASTools workflow to reusable caller mode.
3. Move orchestration primitives (`AgentRequestAnalysis`, analyzer, router, orchestrator catalog`) into Elegy.
4. Convert SAASTools endpoints/dispatcher/services to adapters that call Elegy surfaces.
