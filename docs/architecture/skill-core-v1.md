# Skill Core

Agent Skills are standard SKILL.md files in `skills/<skill-name>/SKILL.md` directories. The `elegy-skills` registry loads, validates, lists, searches, resolves, and shows skills from the skills directory.

The skill registry does not project capabilities, MCP tools, invocation templates, or side-effect metadata. It validates YAML frontmatter and serves skill metadata for agent discovery.
