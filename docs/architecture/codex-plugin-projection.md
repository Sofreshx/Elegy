# Codex Plugin Projection

Simplified. Elegy plugins now consist of a minimal plugin manifest plus Agent Skills. Codex plugin export generates `.codex-plugin/plugin.json` and `skills/` directories from the plugin manifest without requiring a portable package intermediary.

See `elegy plugin export codex --help` for current usage.
