{
  "id": "cap.example.echo",
  "displayName": "Example Echo Capability",
  "version": "1.0.0",
  "description": null,
  "family": "skill",
  "tags": ["example", "echo"],
  "input": {
    "schema": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "message": { "type": "string" }
      },
      "required": ["message"]
    },
    "schemaRef": null,
    "description": "Accepts the message to echo back."
  },
  "output": {
    "schema": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "message": { "type": "string" }
      },
      "required": ["message"]
    },
    "schemaRef": null,
    "description": "Returns the echoed message."
  },
  "execution": {
    "sideEffectClass": "none",
    "authMode": "none",
    "idempotence": "always",
    "costHint": "low",
    "latencyHint": "interactive",
    "timeoutSeconds": 30
  },
  "governance": {
    "trustLevel": "trusted",
    "approvalRequirement": "none",
    "policyRefs": []
  },
  "source": {
    "sourceKind": "manual",
    "sourceRef": "contracts/fixtures/capability-definition.minimal.json",
    "artifactRef": null
  },
  "observability": {
    "labels": ["example", "echo"],
    "correlationRequired": true,
    "emitsExecutionEvents": true
  },
  "lifecycleState": "draft"
}
