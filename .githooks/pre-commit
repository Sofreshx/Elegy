#!/usr/bin/env bash
# Pre-commit hook: fast fmt check.
set -euo pipefail

REPO_ROOT="$(cd "$(dirname "$0")/.." && pwd)"

echo "[pre-commit] cargo fmt --all --check"
(cd "$REPO_ROOT/rust" && cargo fmt --all --check)

echo "[pre-commit] OK"
