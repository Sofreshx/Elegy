# Elegy Codegraph

**Readiness: implemented; not agent-routable.** Source extraction and graph
queries work in workspace tests. A clean installed repository-analysis task is
not recorded. Build with `cargo build -p elegy-codegraph`; invoke
`elegy-codegraph extract` and then `elegy-codegraph query`. Evidence covers
source tests and workspace compilation, not an installed consumer workflow.

