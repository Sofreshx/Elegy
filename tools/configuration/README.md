# Elegy Configuration

**Readiness: implemented; not agent-routable.** Catalog listing and governed
profile application work in source tests. No clean installed target-repository
task is recorded. Build with `cargo build -p elegy-configuration`; invoke
`elegy-configuration list` or `elegy-configuration apply`. Evidence is limited
to source and package verification.

