## Elegy Configuration

This repo applies deterministic OpenCode-facing agentic configuration through `elegy configuration`.

- Repo-local skill authority is configurable and mirrored into the selected OpenCode skill root.
- Deterministic hook and MCP config files are materialized from governed templates.
- Verify drift with `elegy configuration verify --profile repo-opencode-minimal --target . --json`.
