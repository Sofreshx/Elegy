# Elegy Documentation

**Readiness: implemented; not agent-routable.** Init, inspect, map, check, and
export work in source tests. Results depend on explicit repository authority
and readiness evidence, and no clean installed repository task is recorded.
Build with `cargo build -p elegy-documentation`; invoke
`elegy-documentation check --project <repo>`. This is a development tool, not a
plugin.

