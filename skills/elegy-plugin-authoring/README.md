# Elegy plugin authoring

**Readiness: concept; not agent-routable.**

What works today: maintainers can inspect the repository-local `SKILL.md` for
the current adapter eligibility, v3 manifest, authentication, projection, and
evidence rules.

Limitations: there is no installed authoring receipt, no non-fixture task
receipt, and this guidance does not prove that a generated package is usable.

Installation: no supported packaged installation is recorded.

Invocation: inspect `SKILL.md` from this source checkout only.

Evidence scope: the canonical `readiness.json` records no qualifying evidence.
This README is a readiness summary, not an independent authority.
