{
  "schemaVersion": "1.3.0",
  "policy": {
    "major": "Increment for breaking schema changes. Requires a bundle major version bump in governance/version-policy.json.",
    "minor": "Increment for backward-compatible schema additions.",
    "patch": "Increment for backward-compatible schema corrections or metadata fixes."
  }
}
